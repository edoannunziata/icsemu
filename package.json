{
  "name": "icsemu",
  "version": "0.1.0",
  "description": "ICS-compatible bridge to Lichess for legacy interfaces.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "bun run src/index.ts",
    "test": "bun test",
    "build:exe": "bun build --target=bun src/index.ts --compile --outfile dist/icsemu"
  },
  "dependencies": {
    "chess.js": "^1.4.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0"
  }
}
